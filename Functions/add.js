// Exercise
// Define a function add which takes two arguments and return their sum.
const add = (num1, num2) => {
	return num1 + num2;
};

add(2, 3); // to run code in node REPL & it'd look like this:
//     > const add = (num1, num2) => {
//     ... return num1 + num2;
//     ... };
//     undefined
//     > add(2, 3);
//     5

console.log(add(2, 3)); //to see output by running file, node add.js
